Problem Statement :

            For a software employee there will be few applications which will be used very often when they started work in the morning. Searching in the "windows search" might will take some time.
        
Resolution :

            Apping is the thing which will get started when computer booted up(Need to add this app in startup apps). In that GUI app there will be applications which will be used regularly. We can directly open from this.
            
           
Requirements :
            It needs one excel file where application names and path of that applications.
            Program fetches information and do the things.
